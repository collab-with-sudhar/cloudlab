sudo apt-get install openjdk-11-jdk
sudo apt install scala
wget https://mirror.lyrahosting.com/apache/spark/spark-3.5.2/spark-3.5.2-bin-hadoop3.tgz
mv spark-3.5.2-bin-hadoop3 spark
nano ~/.bashrc
export SPARK_HOME=/home/osboxes/spark
export PATH=$PATH:$SPARK_HOME/bin:$SPARK_HOME/sbin

source ~/.bashrc

spark-shell

//create a input file with some texts

val inp=sc.textFile("input.txt")
val result=inp.flatMap(w=>w.split(" ")).map(l=>(l,1)).reduceByKey(_+_)
result.collect().foreach(println)